{
  "name": "netflix-clone-remote",
  "version": "1.0.0",
  "description": "Netflix clone with remote video playback",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "mocha tests/**/*.test.js --timeout 10000"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "chai": "^4.5.0",
    "mocha": "^10.8.2",
    "puppeteer": "^21.11.0",
    "supertest": "^6.3.4"
  }
}
